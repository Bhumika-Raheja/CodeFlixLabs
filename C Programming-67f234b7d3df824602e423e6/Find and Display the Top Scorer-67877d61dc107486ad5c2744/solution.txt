[{"code":"#include <stdio.h>\r\n#include <string.h>  \r\nstruct Student {\r\n    int rollno;\r\n    char name[15];\r\n    float marks;\r\n};\r\nint main() {\r\n    int n;\r\n    scanf(\"%d\", &n);\r\n    int topRoll = 0;\r\n    char topName[15];\r\n    float topMarks = -1;  \r\n    for (int i = 1; i <= n; i++) {\r\n        struct Student one;\r\n        scanf(\"%d %s %f\", &one.rollno, one.name, &one.marks);\r\n        if (one.marks > topMarks) {\r\n            topMarks = one.marks;\r\n            topRoll = one.rollno;\r\n            strcpy(topName, one.name); \r\n        }\r\n    }\r\n    printf(\"Top Scorer: Roll Number: %d\", topRoll);\r\n    printf(\", Name: %s\", topName);\r\n    printf(\", Marks: %.2f\\n\", topMarks);\r\n    return 0;\r\n}\r\n\r\n","language":"c","time":"2025-05-28T13:14:33.124Z","isCorrect":true}]